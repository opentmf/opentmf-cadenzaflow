# Trivy ignore file - vulnerabilities reviewed and explicitly accepted.
# Applies to both the `docker` Maven profile scans and the release workflow.
#
# Format: one CVE id per line, with a comment explaining why the risk is
# accepted and when to revisit. Currently empty - no accepted findings.
